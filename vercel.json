{
  "framework": "nextjs",
  "installCommand": "npm install --prefix matematik-platform && ln -sfn matematik-platform/node_modules node_modules",
  "buildCommand": "npm run build --prefix matematik-platform && ln -sfn matematik-platform/.next .next",
  "outputDirectory": "matematik-platform/.next",
  "devCommand": "npm run dev --prefix matematik-platform",
  "regions": ["fra1"],
  "crons": [
    {
      "path": "/api/cron/live-lesson-reminders",
      "schedule": "0 6 * * *"
    },
    {
      "path": "/api/cron/worksheet-candidates",
      "schedule": "10 3 * * 1"
    }
  ]
}
